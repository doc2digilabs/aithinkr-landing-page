import { Auth } from "@/components/auth/Auth";
import { Layout } from "@/components/layout";

const AuthPage = () => {
  return (
    <Layout>
      <Auth />
    </Layout>
  );
};

export default AuthPage;
